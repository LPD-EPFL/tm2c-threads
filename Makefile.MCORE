PLATFORM_CFLAGS =
PLATFORM_LDFLAGS = 
PLATFORM_LIBS = -ltask -lrt -lzmq
PLATFORM_DEFINES = -DPLATFORM_MCORE \
		   -DUSING_ZMQ \
                   -UPGAS

HAS_TASKLIB = no

ARCHIVE_SRCS_PURE += mcore_malloc.c
ARCHIVE_SRCS_PURE += sys_MCORE.c

# same as cluster, non-pgas experiments will not work
ALL_BMARKS = bank
